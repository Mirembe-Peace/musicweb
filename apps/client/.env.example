# API URL — must be set before building for production
# This is baked into the build at compile time (Vite replaces it)
#
# Development:
VITE_API_URL=http://localhost:3000
#
# Production example:
# VITE_API_URL=https://api.ashabamusic.com
